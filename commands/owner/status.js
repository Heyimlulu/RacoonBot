/*
module.exports = {
    name: 'status',
    description: 'Status!',
    aliases: ['status'],
    category: 'owner',
    args: [
        {
            id: 'status',
            prompt: 'which status should i have?',
            type: 'string',
            match: 'rest'
        }
    ],
    execute(message, args) {

        client.user.setActivity(args.status);
        message.channel.send(`Status have been set to ${args.status}`)
    },
};

 */